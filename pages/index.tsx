import CommonButton from '@/components/common/Button/CommonButton';

const IndexPage = () => {
  return (
    <div>
      full page
      <CommonButton />
    </div>
  );
};

export default IndexPage;
